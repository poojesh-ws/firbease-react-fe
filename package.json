{
  "name": "react-firebase-monorepo",
  "description": "A React firebase monorepo setup poc",
  "engines": {
    "npm": ">=5",
    "node": ">=8.15.1"
  },
  "scripts": {
    "start:web": "cd web && yarn start",
    "test:web": "cd web && yarn test"
  },
  "workspaces": [
    "web"
  ],
  "dependencies": {
    "concurrently": "4.1.0"
  }
}
